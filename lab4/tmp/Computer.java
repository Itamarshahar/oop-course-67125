package lab4.tmp;

/**
 * The `Computer` class represents a computer system, which extends the
 * `ElecricalAppliacne` class. It has the following properties:
 * - `os`: the operating system of the computer
 * - `motherBoardModel`: the model of the computer's motherboard
 * - `cpuModel`: the model of the computer's CPU
 * It also provides a `compute()` method to perform computations.
 */
class Computer extends ElecricalAppliacne {
    OperationSystem os;
    String motherBoardModel;
    String cpuModel;

    /**
     * Performs a computation on the computer.
     */
    void compute() {
    }
}
